# make it top line for unix system for it to choose ruby interpreter
# shebang line - #!/usr/bin/env ruby
# .rb extension is common for ruby files

# exit running script
exit or exit! # exits with status code 0 (success) or non-zero (failure)
abort(msg) # raises an exception and exits the script
# ctrl-c # interrupts the script (raises Interrupt exception)
